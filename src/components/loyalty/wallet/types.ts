export interface WalletItem {
  id: string;
  shopCoverImage: string;
  shopName: string;
  shopDescription: string;
  shopAddress: string;
  voucherTitle: string;
  stamps: number;
  threshold: number;
}