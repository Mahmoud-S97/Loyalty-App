import { JSX } from "react";
import { TouchableOpacity } from "react-native";

const MainButton = (props: any): JSX.Element => {
    return <TouchableOpacity {...props} />
}

export default MainButton;