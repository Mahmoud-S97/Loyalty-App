
export type WalletItem = {
  id: number,
  title: string,
  name: string,
  image: any,
  address: string,
  points: number,
  threshold: number,
}

export type NotificationItem = {
  id: number,
  icon: any,
  title: string,
  body: string,
  createdAt: string
}